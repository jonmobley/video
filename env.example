# Supabase Configuration
# Add these to your Netlify environment variables
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_public_key

# Admin Security
# Generate a strong random token for admin operations (e.g., openssl rand -hex 32)
ADMIN_TOKEN=your_secure_random_token_here

# CORS Security (Optional)
# Restrict admin endpoints to your domain only
# Example: https://your-site.netlify.app or https://yourdomain.com
# Leave empty or set to * to allow all origins (less secure)
ALLOWED_ORIGIN=
